services:
  - type: web
    name: portfolio-contact-form
    runtime: node
    rootDir: contact-form-backend
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
